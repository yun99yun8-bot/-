services:
  - type: web
    name: tron-monitor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 2 --threads 4 --timeout 45
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: RUN_EMBEDDED_WORKERS
        value: "0"

  - type: worker
    name: tron-monitor-worker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python worker.py
    envVars:
      - key: DATABASE_URL
        sync: false
