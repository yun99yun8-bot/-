# TRON Monitor V9.4.1 — 0尾区块校准

## V9.5.2 多类特征研究与纯历史逐期验证
- 已完成期次最多取2500期；从截至目标期开始之前的历史结果、当期第1～17组各位置单几、当期已出现的哈希字符形态、以上综合特征四类预设候选中调参选择。目标第20组哈希/结果只作为训练标签，不参与该期特征构建。
- 按时间顺序划分70%训练、15%调参、15%留出：在调参段选候选，最后留出只做验收。留出的前后两段必须分别在第一名和预测误差上超过基础分布，总第一名至少多3期，才会把训练模型加入正式综合预测。未通过不强行启用。展示四类候选的留出第一名对比，但不依据留出排名重新选择赢家。
- 新增独立的 `history_only` 研究记录：当期预热历史数据后，后台尝试在目标区块前保存纯历史第一名、分数、锁定时间及训练截止期。AI验证统计独立展示这一路的有效期数、第一名命中、固定单3对照与迟到期数。它不会修改第17组正式锁定记录，也不会因历史回测结果自动变成正式选择。
- V9.5.1 的第17组逐段时间线和经确认的360期尾数切换保持。旧模型快照按原有7个特征解析；新模型需后台首次训练完成后才显示四类对比。不能以历史留出成绩宣称上线命中率已提升或第一名能达到80%。

## V9.5.1 第17组锁定时间线
- V9.5.0 的线上截图显示第1376、1377期锁定分别晚于目标区块1.21、1.31秒；因此旧版没有完成开奖前锁定。
- AI验证统计的最近运行状态增加同一期时间拆分：第17组链上时间到目标区块、第17组链上时间到本进程首次发现、到开始尝试锁定、尝试到数据库保存，以及保存相对目标区块。不同线程抢先写入时，“尝试→写库”只代表该事件的相对时间，应结合来源和其他字段判断。
- 第17组事件锁定优先使用内存中的17组原始区块，缺组才查询数据库；预测保存先于诊断追踪写入，避免监控开销侵占锁定窗口。
- 360期尾数切换、开奖结果和预测判定条件均未更改。迟于链上目标区块的记录仍不能计入有效命中率。此版本尚无线上新期次证据，不能宣称已修复延迟或达到80%第一名命中率。

基于 V8.1.6 稳定采集/Worker/遗漏引擎，重建 AI 决策层。

## V9 核心
- 严格只使用开奖前可见的 1–17 组与此前正式开奖结果。
- 修正旧模型历史序列方向：历史数据为“最新→最旧”，近期窗口、遗漏与转移模型均按正确方向计算。
- 同时运行 long / short / structure17 / transition / omission / binomial 六个候选模型。
- 候选模型权重由过去已锁定且已验证的样本外成绩自动调整，并对小样本做收缩。
- Binomial(7,0.5) 作为简单基准，避免把单3/单4天然高频误认为 AI 规律。
- 每期预测仍在 G20 前持久化锁定；G20 只负责验证，不能反向修改预测。
- ensemble_detail 中保存 V9 权重、候选组件和 rolling-OOS 研究成绩，可继续审计。
- `edgeStatus` 会标记 NO_EDGE / WEAK_EDGE / EVIDENCE；它只是统计证据等级，不代表保证命中。

## 保留
V8.1.6 的 G17 事件锁定、G20 屏障、DB-first、自修复、独立遗漏引擎和 Render Background Worker 架构均保留。

## 部署
Web 与 Background Worker 继续使用同一仓库和 DATABASE_URL。Worker Start Command: `python worker.py`。


## V9.1 多维AI决策中心
- 首页重构为最终综合结论、Top3、多模型预判、真实样本外命中反馈和基准比较。
- 候选模型扩展为17组结构、哈希结构、位置关联、短期、长期、状态转移、遗漏条件、Binomial基础分布。
- 哈希结构只作为开奖前统计特征，不宣称存在可预测公式；真实样本外成绩可增加其探索权重。
- 最近20/100/500期Top1实时反馈仅使用开奖前已锁定并已开奖验证的记录。
- 保留V9.0/V8.1.6的G17/G20生命周期、数据库与遗漏引擎。

## V9.2 验证修正
- 样本外统计只使用 V9 预测记录，且必须能核对 `locked_at <` 目标区块的链上时间；早期版本及不能核实锁定顺序的记录不计入。面板最近20/100/500期与模型成绩采用相同筛选。
- 每个复杂模型在与基础分布相同的已验证期次上计算 Top1 配对优势及抽样误差。至少100个有效样本且优势超过1.64倍抽样误差时才获得额外权重。证据提示为探索性统计，不能当作未来命中保证。
- 历史窗口默认读取最近1000期，和长期模型描述一致。界面展示每个候选模型的有效样本数，移除了未经校准的“一致度”百分比。
- 开发检查：`python -m unittest discover -s . -p 'test_*.py' -v`；生产运行仍使用 `render.yaml` 中相同的 Web/Worker 和 `DATABASE_URL`。

注意：这份包尚未连接用户的线上 PostgreSQL 或 TRON 服务进行集成验证，部署前请检查实时锁定事件与区块时间记录。

## V9.3 单3/单4集中问题
- V9.2 样本不足时基础分布占100%，因此最终排序会退化为单3、单4。V9.3 使用固定且明确标为“探索性”的多维初始权重：基础分布25%、前17组结构20%、历史位置关联15%、短期12%、长期8%、转移8%、遗漏6%、哈希结构6%。
- 验证过的配对优势仍能追加有限权重；没有统计优势时，页面会明确显示“探索性排序”。强烈的当期结构信号能够让单2等进入综合第一位，但不会为了分散结果而强制轮换号码。
- 锁定后的 Top3 显示来自同一个冻结快照的分数和百分比，避免展示旧算法分数。

## V9.3.1 首页“单—”修复
- 首页在展示正式七号码前核算单几；即使缓存或接口的单几字段为空，也从号码尾数重新计算。无有效七号码时显示“待开奖”，不把未知值误显示成开奖结果。
- 新页面刚打开且当期尚未开奖时，从最近已确认的第20组结果恢复上一期的期号、七号码、单几及区块。快速结果接口在当前期尚未开奖时也提供这一回退；当期正式结果出现后仍由新区块覆盖。
- 前端脚本语法、单几为0或字段缺失的渲染路径及现有模型测试已检查；仍需在实际部署环境核对数据库中的历史区块。

## V9.3.2 采集速度
- 第20组快速线程在前17组尚未到齐、且倒计时仍大于12秒时暂停请求尚不存在的目标区块；到第17组或倒计时12秒内立即恢复直接查询。减少与正常采集线程争用共享请求限速器，避免无效请求引发429。
- 支持后端环境变量 `TRON_PRO_API_KEY`；只向 `api.trongrid.io` 发送 `TRON-PRO-API-KEY` 请求头，不暴露给浏览器或其他数据源。未设置时保持原有请求方式。
- `/api/draw` 的 `debug.fastTarget` 新增 `barrierMs`，区分目标取块耗时 `latencyMs` 与开奖前预测锁定耗时。若 `barrierMs` 大，继续排查 G17 提前锁定；若 `latencyMs` 大，检查节点响应与429。
- 区块本身未产生前不能提前取得开奖结果。升级后应在真实线上运行中比较 `latencyMs`、`barrierMs`、429次数和平台显示时间，本地测试无法证明实测提速。

## V9.3.3 首页波色
- 删除首页“本期正式结果已确认”及同一状态栏的提示文字，保留开奖期号、区块、倒计时与七个号码。
- 七个开奖号码改为白底彩色圆形描边：绿波 05、06、11、16、17、21、22、27、28、32、33、38、39、43、44、49；蓝波 03、04、09、10、14、15、20、25、26、31、36、37、41、42、47、48；红波为其余给定的17个号码。
- 新结果到来时按号码重绘，先清除旧波色，避免上期颜色留在下一期。占位号码保持灰圈。

## V9.3.4 单3/单4诊断与全类别显示
- 用固定种子的1500组二项基准模拟复现原模型：Top1 单3为756次、单4为735次，单2仅2次、单5仅7次。该测试检查代码偏置，不是线上命中率或未来预测能力的证明。
- 首页在锁定后展示单0–单7全部相对模型分数，并单独列出分数最高的非3/4类别。它是研究候选，可与正式Top3重合，不额外计入命中。
- 正式锁定预测和样本外评价保持原口径；没有验证证据时不会为了号码分散而强制轮换 Top1。若要改变正式选择策略，应先用真实的已锁定历史记录做逐期回测，评估命中率和覆盖范围。

## V9.4 历史哈希训练
- Background Worker 每5分钟从 `period_groups` 中读取最多2500个已完成期次（约4.5万组）。只把每期1–17组哈希及其单几作为特征，第20组的单几只作训练标签。使用哈希末尾、A–E与数字构成、17组单几结构等低维特征，不记忆完整哈希。
- 按期号先后划分70%训练、15%调参、15%留出验证；至少400个完整期次才训练。留出期次的对数损失和Top1都优于简单基准时，模型快照才加入正式集成预测。否则只显示“未达门槛”，正式预测不增加哈希模型权重。
- 训练和验证在后台进行，保存于 `hash_model_runtime`。第17组锁定时只读取快照，不扫描几万条哈希；预测使用的快照及分数写入本期已锁定记录，保持第20组开奖后不可倒改。
- 首页显示训练样本数、留出验证期数与模型是否入选。此版本仅通过合成数据和代码测试，未接入用户线上 PostgreSQL，因此不能声称已发现哈希的可预测规律或提高命中率。

## V9.4.1 平台0尾校准
- 用户截图确认 2026-09-25 第0266期 = 86536400，直至第0275期 = 86536580，每期+20。该区间和未来目标区块统一改以第0266期作为新锚点；第0276期预计为86536600。
- 旧版对第0275期算出86536582，导致本期目标落后平台2块。2026-09-24 第0481、1001、1200→1201期的已确认锚点保留。
- 新跳变发生在2026-09-24第1201期之后、第2026-09-25第0266期之前，但截图无法确定确切期数。对于这段未确认历史，不声称已经校准；后续可由更多平台区块号补齐。

## V9.4.2 尾数平均切换日程（推算）
- 已确认 2026-09-24 第0481期 8→6尾、第1201期 4→2尾。中间经过 6→4尾两次切换，因此 (1201−481)÷2＝360期，按每期一分钟推算每次相隔6小时。
- 推算的边界：09-24 第0841期 14:01 为 6→4；09-25 第0121期 02:01 为 2→0；09-25 第0481期 08:01 为 0→8；后续按同样间隔和 8→6→4→2→0→8 循环自动划分。第0480/0481期和第1200/1201期的实际区块仍按已确认数据校验，第0266至0275期的0尾与截图一致。
- 自动目标区块在每个预计边界应用一次 +18 而非 +20；页面显示本期尾数、预计下次切换的日期/期号/北京时间，并提示推算边界。未知边界尚未经平台逐期核实；若实际切换不按360期，预计边界附近及之后的目标区块会偏移，需用平台截图或真实映射重新校准。

## V9.4.3 开奖结果与记录修复
- 后台上线后检查最近5000个已保存的正式期次；当旧组20目标区块与新尾数日程不一致时，从已保存的原始区块重新构建20组及正式结果，清除该期旧映射的组次数据，随后重算独立遗漏统计。若缺少新目标区块，保留原始区块档案，不编造开奖结果，同时将旧预测的验证标记清空。
- 历史页开奖结果只显示当前映射下能从原始区块查到的真实七号码和单几；旧目标区块上的预测显示“旧区块记录”，不列为命中。其他预测只有目标区块一致、已保存结果一致、锁定时间早于实际区块时间才列为已验证。
- 在线预测验证也要求该期目标区块和实际开奖区块一致；历史哈希训练过滤旧目标区块记录。库中的原始区块档案继续保留。上述重建需有部署环境中的历史 PostgreSQL 原始区块；本地无线上数据库，只完成代码测试。

## V9.4.4 开奖显示快路径
- 第20组原始区块走独立数据库写入队列和专用线程，不再排在1～19组普通区块写入队列后面。Web进程仍只读取已真实取得的区块，不能提前猜测哈希。
- 第17组预测已持久锁定时，本进程记住其期号；第20组获取后直接发布，无需再次等待数据库检查。若未提前锁定，继续执行原有开奖前屏障，避免开奖后倒填预测。
- 首页已显示上一期结果时，快速开奖接口不再每450毫秒重复查询前3期，减少与正式结果查询竞争。Worker心跳记录 `fastTarget` 和 `resultWriterBacklog`，部署后可用系统健康接口查取块耗时、屏障耗时与写入拥堵。
- 本地验证包括正式结果优先队列及已锁定屏障短路；无线上数据库和平台同步测量，无法保证实际比平台早显示或固定减少5秒。若耗时仍集中在区块接口/429/4秒时钟偏移，应按线上诊断结果继续调整。

## V9.4.6 AI训练与命中证据诊断（保留原360期切换）
- 历史数据 → AI验证统计新增最近1000条预测诊断，可区分：保存的预测条数、可验证的预判样本、Top1/Top3真实命中、单3固定对照命中，以及旧目标区块、无原始区块、开奖后锁定、未写入验证、实际单几冲突等记录问题。最近20/100/500期卡片也分别标注第一名和前三名，避免把第一名的0%误读为前三名的0%。
- 从持久化训练快照读取真正用于哈希训练的完整期数、调参期数、留出验证期数和入选状态。1000期开奖记录并不必然等于1000个拥有完整前17组+第20组的训练例。训练模型需留出验证优于基础分布才参与正式预测；诊断不会强制启用不达标模型、回填开奖后预测或伪造胜率。
- 浏览器打开历史数据中的AI验证统计，点击刷新可复查；同一诊断可在 `/api/ai-audit?limit=1000` 读取。仅核查程序自身期号、目标区块和真实链上记录；没有用户线上数据库及平台对应期开奖原始记录，无法在本地证明平台实际对齐或模型已提高命中率。360期尾数循环保持V9.4.4原样。

## V9.4.7 AI证据加权
- 最近500条符合目标区块映射、且确实在开奖前锁定的记录用于比较各维度与基础分布在同一期的第一名成绩；旧目标区块映射的预测不参与模型权重学习。
- 有至少200条配对有效样本、领先基础模型至少2个百分点且超过2.58倍抽样误差的维度才增加权重；显著落后的维度降低权重。历史哈希训练模型仍须先通过独立留出期次检验才参与合成。
- 首页每个模型标示“探索权重”“验证加权”或“验证降权”。不强行轮换单几；若真实开奖前信息对其余类别没有可复现优势，单3和单4仍可能经常排在首位。测试使用合成样本，只验证算法路径，不代表实际命中率提高。
- 360期尾数切换及此前确认的期号映射保持原样。

## V9.4.8 线上单3/单4诊断
- AI验证统计新增当前程序版本、最近一次已锁定预测版本、当前版本有效验证期及真实命中数，能核查更新后旧预测是否被误认为新版表现。
- 显示有效期中单0至单7的预测分布和实际分布，并保留目标区块错位、未锁定、无原始区块等无效原因。若约1000期几乎零命中，先核对页面有效样本及平台同期期号和七号码，再判断是否值得改变模型。
- 此版本没有线上真实开奖数据，不能证明存在可预测的哈希规律。360期尾数切换维持不变。

## V9.4.9 第17组预测锁定修复
- 线上用户诊断显示最近1000条历史预测仅3条可验证，其余997条锁定晚于目标区块；历史哈希模型虽有1192个完整期、179期留出验证，仍未通过验证。三条有效样本不能证明实际命中率。
- Background Worker在每期开始预先计算历史期次、位置关联、配对验证成绩和哈希模型快照。第17组出现后的关键路径不再重复扫描历史数据，也不再先额外写入17组结论；预热未完成时稍后重试，不冒充已提前锁定。
- 目标区块一经采集即拒绝新增预测；数据库写入也原子地拒绝已保存目标区块。积压重试遇到开奖结果直接丢弃并记录错过期次，不倒填历史锁定时间。线上仍需通过链上区块时间验证每条预测；网络延迟可能造成少量写入后发现已迟到的记录，这些记录继续由严格审核排除。
- AI验证统计新增迟到记录的锁定延迟中位数、最小值与最大值，方便部署后判断是计算变慢还是目标区块/时间对齐偏移。旧的997条不篡改，只有部署后新增有效期次才能评估修复效果。360期尾数循环不变。

## V9.5.0 取消第20组开奖后的补锁
- 用户线上V9.4.9诊断证实网页和最近预测均为新版，但最近1000条仍只有3条有效、997条迟到，中位迟到1.2秒；本版本排查发现目标区块快路径在已经取得第20组区块之后还执行“发布前屏障”，可能在开奖后计算并写入新预测。这不是有效的提前锁定。
- 快结果线程取得目标区块后立即发布，不再调用补锁预测；历史迟到记录不回填，不能靠把链上时间改晚来增加命中。独立第17组快速线程最多尝试4次采集第17组区块并触发锁定；查询共享限速器，不在剩余5秒内争抢开奖结果请求。
- 统计同时显示网页版本、Background Worker版本、当前版本保存/有效/迟到数，以及最近期次锁定时间差和运行状态。只有当前版本新增有效记录才能证明锁定修复。旧997条仍在1000条滚动统计中时，总数暂时会看起来一样。360期尾数循环继续保持原样。
